{
  "description": "Nudge the user to run /cc-cream:setup while the status bar isn't wired yet.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/setup-reminder.js\""
          }
        ]
      }
    ]
  }
}
