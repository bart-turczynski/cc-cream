---
description: Remove cc-cream's statusLine wiring from your settings.json. Run /plugin uninstall cc-cream afterwards to drop the plugin.
allowed-tools: Bash(node:*)
---

!`node ${CLAUDE_PLUGIN_ROOT}/src/install.js --uninstall`
